// config.h
// This file is generated by cmake from config.h.in
// Things that may change based on the build directory, such as paths to files
// and the file version are procedurally generated by cmake here

// Define component version
#define ${CMAKE_PROJECT_NAME_UPPER}_VERSION_MAJOR ${${CMAKE_PROJECT_NAME}_VERSION_MAJOR}
#define ${CMAKE_PROJECT_NAME_UPPER}_VERSION_MINOR ${${CMAKE_PROJECT_NAME}_VERSION_MINOR}
#define ${CMAKE_PROJECT_NAME_UPPER}_VERSION_PATCH ${${CMAKE_PROJECT_NAME}_VERSION_PATCH}

// Define resource folder for UT suite
// This path points to the folder with sample .cfg files
#define TEMPLATE_UT_RESRC_PATH "${PROJECT_SOURCE_DIR}/test/cfg"
