// template.h
// Example header file that defines symbols that go in a library later

struct Template {
    // Data members
    int a;
    int b;

    // Member functions
    // Calculates a + b
    int sum();

    // Calculates a / b
    int divide();
};
